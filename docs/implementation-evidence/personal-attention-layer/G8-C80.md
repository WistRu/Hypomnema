# G8 C80 evidence — research schema and atomic publication primitives

Status: **PASS**  
Date: 2026-08-13  
Baseline HEAD: `40f5297250cfd214d7fbac2639c013228044e56d`  
Accepted schema transition: `23 -> 24`

## Accepted packet

`C80` establishes the captured-only research foundation:

- closed shared target, corpus, budget, report, evidence, privacy and typed-error
  contracts;
- always-applied migration `024_research_workflow.sql`;
- deterministic current-corpus rebuilding with exact UTF-8 source chunks and
  bounded shareable snapshots;
- atomic run/job/corpus submission and atomic report/evidence publication;
- append-only state/head projections and research-owned privacy redaction;
- fixed research concurrency, UTC-day attempt and cost reservation limits.

Preflight REST, approved-corpus reading, worker/provider execution, report
read/refine, web UI and MCP research tools are not part of C80.

## Independent acceptance audit

The fourth fresh read-only audit accepted the current tree:

```text
P0/P1/P2/P3:       0/0/0/0
mandatory UNVERIFIED: 0
verdict:            PASS
```

It independently verified:

- fresh schema 24 and `23 -> 24`, XOR/selection/FK/unique/immutability guards;
- stable `101 -> first 100` inclusion and exact `10,001` rejection as
  `RESEARCH_SCOPE_TOO_LARGE` with zero durable writes;
- server-owned manifest rebuild, exact replay, stale rejection and rollback;
- code-point-safe `captured_text_chunks:v1`, exact digests, empty-text typed
  exclusion and the 4 MiB corpus ceiling;
- unsafe/internal/private/credentialed URL exclusion;
- all four snapshot kinds, privacy fencing, superseded-report redaction and
  attempted publication after cancellation producing no report;
- evidence-subset enforcement, empty/failed report rules, caller-supplied
  report-id bypass closure and atomic publication rollback;
- exact ten-attempt UTC-day and USD 2 spent-plus-reserved scheduling caps;
- schema-24 feature-off recovery/cancel/write fencing with the bound running
  job preserved, `integrity_check=ok` and zero FK violations.

Boundary remediations were also exercised dynamically:

- a valid 200-byte public privacy idempotency key derives stable bounded
  `privacy-event:v1:<sha256>` child keys and exact replay performs no writes;
- the exact valid browser title `"界".repeat(2048)` persists unchanged at
  6,144 UTF-8 bytes; 2,049 characters are rejected by the shared boundary;
- 60,000-character quote-heavy page context, Resource context and relation
  note snapshots are truncated at Unicode code-point boundaries against the
  final canonical JSON byte size, remain at most 65,536 bytes, expose their
  truncation flag and hash the exact stored material;
- the built MCP runtime entry resolves the shared title limit through the
  public `@tabhub/shared/limits` export.

Independent focused results on the accepted tree:

- shared research contracts: `8/8`;
- focused C80 server: `44/44`;
- durable ledger: `36/36`;
- MCP stdio: `3/3`;
- direct `node packages/mcp/dist/main.js`: exit `0`.

## Full regression and build

```text
corepack pnpm test
shared:     50/50
extension: 321/321
mcp:       108/108
server:    648/648
web:       477/477
aggregate: 1,604/1,604 PASS

corepack pnpm typecheck
PASS for all five packages

corepack pnpm build
PASS for all five packages

git diff --check
PASS (line-ending notices only)
```

## Live-state noninterference

The user runtime was not restarted, migrated or written:

- PID `581820`;
- command `"C:\Program Files\nodejs\node.exe" D:\VibeCoding\TabHub\packages\server\dist\main.js`;
- health `ok`, database `ok`, schema `17`;
- baseline HEAD remains `40f5297250cfd214d7fbac2639c013228044e56d`;
- quarantine baseline SHA-256 remains
  `52ff4898270f40375e103bae6c7ec3d989fdfe9b082552dbb2b1e3bf20348016`.

## Corrective reissue

The later C82 readiness audit found that the full implementation plan required a
separate report FTS namespace which the original migration 024 did not yet
contain. Because schema 24 had not been rolled out to the user's schema-17
runtime, corrective packet C80r amended migration 024, re-proved fresh and
`23 -> 24` migrations and rederived Q10K-G8. The accepted correction and current
hashes are recorded in [G8-C80r.md](G8-C80r.md); this original record remains the
audit history of the first C80 acceptance.

**C80 and its C80r corrective reissue are accepted. C82a is the next ready
packet; G8 itself remains active.**
