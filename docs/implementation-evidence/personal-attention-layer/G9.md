# G9 bounded live acquisition gate

Status: **PENDING — implementation accepted; mandatory live J3 and user path not complete**  
Date: 2026-08-21  
Schema head: `26`

G9 cannot yet be marked `PASS`. The implementation, privacy boundary, UI and
automated regression are accepted, but the gate formula also requires one real
public-HTTPS acquired-evidence journey, a real extension fallback/tab-invariance
receipt and user review of a personally relevant Resource.

## Completed evidence

- Contract and schema boundary: `G9-contract-freeze.md`, `G9-C90a.md`,
  `G9-C90a-R2a.md`, `G9-C90a-R2b.md`, `G9-C90a-R3.md`.
- Schema-026/R5 purge correction: `G9-C90a-R5.md`.
- Acquisition runtime and handoff: `G9-C90b-V2.md`,
  `G9-C90b-V3-V5.md`, `G9-C90b-final.md` and
  `G9-C90b-read-projection.md`.
- W90 UI: `G9-W90.md`, final independent verdict
  `P0=0, P1=0, P2=0, UNVERIFIED=0`.
- Full regression: `G9-regression.md`, `2,435/2,435` tests plus workspace
  typecheck/build/diff-check, all exit code 0.
- Isolated schema-24 to schema-26, fresh schema-26 and feature-off rollback:
  `G9-isolated-migration-rollback-receipt.json`, verdict `PASS`.
- Deterministic public fixture: `G9-r-uat12-v1-receipt.json`, verdict `PASS`.
  A fresh schema-26 database was built through the production Resource,
  logical-page and tab catalogs. It has 12 public IANA pages, exactly six
  captured and six missing; all 12 returned status 200 and usable HTML through
  the production `SafePublicHttpClient` with one DNS call and one pinned IPv4
  connection per URL. The pristine hashed database is kept separate from the
  mutable runtime copy used by the extension.
- Isolated extension harness: `G9-extension-7718-harness-receipt.json`, verdict
  `PASS_ISOLATED_SMOKE`. The user loaded the port-7718 build. Protocol-v5 relay
  connection, browser-state read and exact activation of the existing TabHub
  tab succeeded; total tabs stayed at 475 and the exact target remained a
  singleton. The working 7717 runtime was not interrupted. The typed
  fallback is continued in `G9-extension-captured-only-fallback-receipt.json`.
- Public-SPA exact capture and captured-only preflight:
  `G9-extension-captured-only-fallback-receipt.json`, verdict
  `EXACT_CAPTURE_AND_CAPTURED_ONLY_PREFLIGHT_PASS_PROVIDER_DOSSIER_PENDING`.
  The production safe HTTP client returned `INSUFFICIENT_PUBLIC_TEXT` for the
  Compound SPA. The exact navigation fence rejected the stale root URL; a
  second command captured 2,044 rendered characters from the stable physical
  tab. Production preflight then reported two Resource pages, one captured and
  one missing. Two temporary test tabs were closed and the browser returned to
  22 windows and 475 tabs. No authenticated user content or wallet connection
  was used.

## Current live-gate boundary

`G9-live-gate-receipt.json` records a fail-closed readiness attempt on an
isolated schema-26 runtime. The requested live-acquisition flag projected as
effectively disabled because no Anthropic research provider configuration was
available. No J3 action was started. That historical readiness PID was stopped.
The current isolated schema-26 runtime is PID 692804 on port 7718, backed by a
mutable copy of the pristine R-UAT12 database; the loaded test extension is
connected to it. The working PID 581820 on port 7717 and its live database were
not changed.

The copied collection's YouTube Resource remains unsuitable because
challenge/SPA behavior does not guarantee usable public content. The isolated
`R-UAT12-v1` replacement is now materialized and production-policy-probed, so
fixture availability is no longer a J3 blocker. The remaining J3 prerequisite
is the local Anthropic provider and immutable pricing configuration.

## Required before PASS

1. Configure the local Anthropic research provider and immutable pricing values
   without writing secrets into evidence or chat.
2. Use accepted `R-UAT12-v1` to complete real server egress, acquired
   materialization, immutable handoff and successful final dossier with
   acquired provenance.
3. Record the complete negative safety matrix together with exact network
   counters and pre/post tab/window identity showing zero mutation.
4. After provider configuration, run the already approvable captured-only
   fallback preflight to a final dossier; exact capture itself is complete.
5. Ask the user to review a personally relevant Resource/dossier. A pre-sample
   UAT waiver is allowed only before that review and cannot waive technical J3.

Final Gate and R99 have not started. No deployment or UAT waiver is recorded.
