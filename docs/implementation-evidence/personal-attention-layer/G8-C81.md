# G8 C81 evidence — preflight, approved corpus and privacy/REST boundaries

Status: **PASS**  
Date: 2026-08-14  
Baseline HEAD: `40f5297250cfd214d7fbac2639c013228044e56d`  
Schema head: `24` (the user runtime remains schema `17`)

## Accepted packet

`C81` completes the captured-only research intake boundary:

- one deep `ResearchWorkflow` owns `preflight`, `requestRun`, `cancel` and
  public `redact`; routes do not rebuild manifests or read corpus tables;
- the complete safe preflight projection, exact corpus fingerprint, estimate,
  budget and confirmation sets are bound by one canonical approval hash;
- the internal-only `ResearchCorpusReader` materializes only the approved
  immutable corpus and shares one SQLite writer boundary with privacy
  redaction;
- public REST routes enforce local capability, exact feature-off behavior,
  closed validation and typed 404/409/413/422 errors;
- schema 24 constructs one ResearchStore, one unified AiJobLedger and one
  DurableAiJobs facade; shared job routes are registered once;
- `Q10K-G8-v1` proves bounded, write-free Resource preflight at 1/100/10,000
  manifest rows.

Provider execution, report history/detail/refine and worker runtime remain
outside C81.

## Acceptance remediations

The independent audit found and the accepted tree closes these gaps:

- real missing page/resource and invalid selection paths now return exact
  `RESEARCH_TARGET_NOT_FOUND` / `RESEARCH_SELECTION_INVALID`, rather than a
  generic validation error;
- a current Resource with no members returns an actionable empty preflight;
  run approval alone returns `RESEARCH_CAPTURE_REQUIRED` with zero writes;
- missing and unavailable parents retain distinct 404/409 semantics at the
  transactional submit recheck;
- duplicate or unsorted run confirmation digests reach the workflow and return
  `RESEARCH_PREFLIGHT_STALE` with zero writes;
- the 4 MiB corpus cap is a deterministic prefix latch: once an otherwise
  eligible source cannot fit, no later smaller source can enter the corpus;
- public target redaction validates missing targets and selection identity
  before any privacy write, while existing targets may record an idempotent
  zero receipt and historical runs remain redactable after Resource merge;
- research cancellation is routed through the same ResearchWorkflow seam;
- the MCP stdio feature expectation includes the default-off `research` flag.

Real `createApp`/SQLite regressions cover these paths, including zero deltas in
`ai_jobs`, `research_runs`, job idempotency receipts, privacy requests and
privacy receipts on rejected operations.

## Independent acceptance audit

```text
P0/P1/P2/P3:          0/0/0/0
mandatory UNVERIFIED: 0
verdict:               PASS
```

The final read-only audit verified 11 focused files (`87/87`), full shared
(`58/58`), full server (`698/698`), both typechecks, real route probes, the
deep workflow seam, WAL/privacy exclusion, deterministic corpus prefix and the
current strict Q10K run.

## Q10K-G8-v1

Current-code strict rerun:

- retained directory:
  `.tmp/personal-attention-layer/g8-c81-strict-20260814T0642/`;
- result SHA-256:
  `a35c445b1a7fdccbe115634774b3c8212590a8f19b6dde8357f1725a2768c172`;
- contract SHA-256:
  `93fc78cbec2800635fd39a95783b4dcab2c6d8855585a07ca6253de956a9c460`;
- accepted source SHA-256:
  `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`;
- primary/replica schema-24 SHA-256:
  `74953a616e8209cae98d948096f6a8a53cfebbf68eed4e711fe26e54d77c4d4d`;
- maximum prepared statements for 1/100/10,000: `8/5/5`;
- write statements and `total_changes` delta: `0`;
- 10,000-row p95: `784.736 ms`, below both `2,000 ms` and the retained
  later-gate threshold `1,330.972 ms`;
- three warmups plus twenty samples, oracle match `23/23`, identical
  derivations, unchanged source/measured DB, integrity `ok`, FK violations `0`.

## Full regression and build

```text
pnpm test
shared:     58/58
extension: 321/321
mcp:       108/108
server:    698/698
web:       477/477
aggregate: 1,662/1,662 PASS

pnpm typecheck
PASS for all five packages

pnpm build
PASS for all five packages

git diff --check
PASS (line-ending notices only)
```

## Live-state noninterference

The user runtime was not restarted, migrated or written:

- PID `581820`, Node running `packages/server/dist/main.js`;
- health `ok`, database `ok`, schema `17`;
- HEAD remains `40f5297250cfd214d7fbac2639c013228044e56d`;
- git index is empty;
- quarantine baseline SHA-256 remains
  `52ff4898270f40375e103bae6c7ec3d989fdfe9b082552dbb2b1e3bf20348016`;
- all three accepted A70 path hashes remain exact.

**C81 is accepted. G8 remains active. Completeness review inserts corrective
packet C80r before the split C82a/C82b implementation.**
