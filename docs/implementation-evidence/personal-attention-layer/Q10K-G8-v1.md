# Q10K-G8-v1 acceptance evidence

Accepted at: `2026-08-13T20:47:00+03:00`  
Gate packet: `C81 / Q10K-G8-v1`  
Result: `PASS`

## Command and retained artifacts

```powershell
corepack pnpm --filter @tabhub/server baseline:g8 -- --work-dir D:\VibeCoding\TabHub\.tmp\personal-attention-layer\g8-official-20260813T2047-final
```

The immutable JSON result, hash evidence and three disposable schema-24
fixtures are retained in
`.tmp/personal-attention-layer/g8-official-20260813T2047-final/`.

## Frozen contract and derivation

- contract: `Q10K-G8-v1`;
- contract SHA-256:
  `93fc78cbec2800635fd39a95783b4dcab2c6d8855585a07ca6253de956a9c460`;
- accepted `S10K-v1` source SHA-256:
  `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`;
- primary and independent replica schema-24 SHA-256:
  `74953a616e8209cae98d948096f6a8a53cfebbf68eed4e711fe26e54d77c4d4d`;
- both derivations are byte-for-byte equal at `29,802,496` bytes;
- schema version `24`, `PRAGMA integrity_check = ok`, foreign-key violations
  `0` for both copies.

The deterministic mixed maximum corpus contains a `10,000`-row manifest:
`9,900` captured, `9,800` eligible, `100` missing and `0` failed. Exactly
`100` sources are included. The fixture provides `120` shareable page-context
snapshots and the preflight returns the closed cap of exactly `100`.

## Authenticated first-party preflight measurements

App construction, resource backfill and local web-session bootstrap occurred
before the timer and statement counter. Each scope then ran three warmups and
twenty measured requests through `POST /api/research/preflight`. Every one of
the `69` responses (warmups included) matched the independent raw-SQL and
locally implemented canonical-SHA-256 oracle exactly.

| Resource scope | Included | Snapshots | p50 | p95 | SQL statement executions (max) | Result |
|---:|---:|---:|---:|---:|---:|---|
| 1 | 1 | 1 | `16.834 ms` | `27.416 ms` | 8 | PASS |
| 100 | 100 | 100 | `23.361 ms` | `45.484 ms` | 5 | PASS |
| 10,000 | 100 | 100 | `956.338 ms` | `1,109.143 ms` | 5 | PASS |

All p95 values are below the introducing-gate limit of `2,000 ms`; every
sample is below the scope-independent limit of `30` prepared-statement
executions. The accepted maximum p95 is `1,109.143 ms`, so the later Final-gate
threshold is `min(2000, max(1109.143*1.20, 1109.143+100)) = 1,330.972 ms`.

## Non-interference and hashes

- measured connection `total_changes` delta: `0` aggregate and `0` maximum per
  request;
- write-statement executions: `0`;
- measured DB before/after WAL checkpoint: identical SHA-256
  `59ac5320fd808aa185ca19475e1059e90eb9646d585dd158dab31c018cbdad76`
  and identical size `35,082,240` bytes;
- measured DB integrity: `ok`; foreign-key violations: `0`;
- accepted source bytes, SHA and mtime: unchanged;
- runner source SHA-256:
  `3a8e14110542b185da4b2512a653e82d877a65a00cb8423b8ff32cc37be0a1f3`;
- result JSON SHA-256:
  `803226fc69ecf2e73f97f8d99d2848f299dd15f93e2a78e8a660dbc681482461`.

The exact hash tuple is also retained in `Q10K-G8-v1.evidence.json` beside the
result artifact.

## Current-code strict rerun after C81 remediation

The complete C81 tree was rerun alone on 2026-08-14 and retained at
`.tmp/personal-attention-layer/g8-c81-strict-20260814T0642/`.

- result JSON SHA-256:
  `a35c445b1a7fdccbe115634774b3c8212590a8f19b6dde8357f1725a2768c172`;
- official/correctness/performance: `PASS/PASS/PASS`;
- maximum SQL executions at 1/100/10,000: `8/5/5`;
- write statements and measured `total_changes`: `0`;
- 10,000-row p95: `784.736 ms`, below the retained Final threshold
  `1,330.972 ms`;
- source/derived/database hash, exact-byte derivation, integrity and FK proofs
  remain unchanged.

## Focused verification

```text
corepack pnpm --filter @tabhub/server typecheck
PASS

corepack pnpm --filter @tabhub/server exec vitest run test/baseline-g8.test.ts
PASS: 1 file, 3 tests
```

## Current-code strict rerun after C80r migration correction

The schema-24 fixture was derived again after the not-yet-live migration 024
gained the required research-report FTS namespace. The immutable rerun is
retained at
`.tmp/personal-attention-layer/g8-c80r-strict-20260814T0655/`.

- result JSON SHA-256:
  `2d4c8da41cc46726bc7aad0fa1f8c0adf1abf16f6988c4e9702e84963331c679`;
- official/correctness/performance: `PASS/PASS/PASS`;
- contract SHA-256 remains
  `93fc78cbec2800635fd39a95783b4dcab2c6d8855585a07ca6253de956a9c460`;
- accepted source SHA-256 remains
  `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`;
- new primary and independent replica schema-24 SHA-256 are equal at
  `2234dd4bcbc91bec1d9b57aff5cc1babd6bb1e5263587a6e0ecf31d71245ded2`;
- new schema SHA-256 is
  `0076053033da59de351fe0021523c745f408e68116aa08751771ba58d30ebc14`;
- maximum SQL executions at 1/100/10,000 are `8/5/5`, write executions and
  per-request `total_changes` deltas are zero, and all 69 oracle checks pass;
- p95 at 1/100/10,000 is `25.226/39.797/863.346 ms`; the 10,000-row value is
  below both the introducing limit and retained Final limit `1,330.972 ms`;
- measured database SHA-256 before and after is the same
  `3b43b4d8bef3656af7a8d193d038930aaeddf0eabd9618353c7ac7f9d02b599a`;
- source bytes/mtime are unchanged, both derivations are byte-identical,
  integrity is `ok` and foreign-key violations are zero.

## Current-code strict reissue after C82b pricing amendment

C82b amended the not-yet-live migration 024 with the nullable, bounded
`ai_job_attempts.pricing_version` field required by research provider binding.
The schema-24 fixture was therefore rederived twice and the complete immutable
Q10K-G8 contract was rerun. The retained directory is
`.tmp/personal-attention-layer/g8-c82b-strict-20260814T0903-final/`.

- result JSON SHA-256:
  `6eb2f8c1a889607d5ae466fe893653979166649d014437b64940e0875aeffc77`;
- evidence JSON SHA-256:
  `a696f7038a7b39cee222b183f8b918a04ed8fe22090a97c484344c1c665d524f`;
- official/correctness/performance: `PASS/PASS/PASS`;
- immutable contract SHA-256 remains
  `93fc78cbec2800635fd39a95783b4dcab2c6d8855585a07ca6253de956a9c460`;
- accepted source SHA-256 remains
  `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`,
  with exact source bytes and mtime unchanged by the run;
- primary and independent replica schema-24 SHA-256 are both
  `d7538fa02572248a5fef533e3a602b1da246d0f60c7386b1b55a799b4f908eb2`;
  both files are exactly `29,835,264` bytes and byte-identical;
- current schema SHA-256 is
  `de2d0d606f7bbe9b257a7f6c0840c976666598dc098e0bf35cffb49e48d569dd`;
- primary and replica both report schema version `24`, integrity `ok` and zero
  foreign-key violations;
- p50 at 1/100/10,000 pages is `11.220/18.226/910.069 ms`; p95 is
  `12.622/21.479/1,028.064 ms`, so every scope is below `2,000 ms` and the
  10,000-page scope remains below the retained Final threshold `1,330.972 ms`;
- maximum prepared-statement executions at 1/100/10,000 are `8/5/5`; all `69`
  warmup/measurement requests match the independent oracle;
- maximum write executions and per-request `total_changes` deltas are zero;
- measured database SHA-256 before and after is exactly
  `9c7dbf99e358feda7adb2d3826d30974a67de0ef3ffe2de254e0e8db833b2b72`
  at `35,115,008` bytes, with integrity `ok` and zero FK violations.

This is the current schema/Q10K evidence for the C82b migration-024 shape. The
earlier C81 and C80r sections remain historical immutable receipts.

## Current-code strict reissue after C82c safe-boundary amendment

C82c amended the not-yet-live migration 024 with immutable, material-free agent
research command receipts and updated the local preflight oracle with the exact
normalized-origin confirmation display. Root therefore rederived both schema-24
fixtures and reran the complete immutable contract. Exactly six artifacts are
retained at
`.tmp/personal-attention-layer/g8-c82c-strict-20260814T095100/`.

- result/evidence SHA-256:
  `dc410ac1582270c5b09645848f1e41135c24654ab3e4947a559869b38acdbe4e` /
  `8e0e4ce82cc487481026f99c3d76f8000fd2429a349996d7c23b37b5e2096a47`;
- official/correctness/performance: `PASS/PASS/PASS`;
- contract/source SHA-256:
  `93fc78cbec2800635fd39a95783b4dcab2c6d8855585a07ca6253de956a9c460` /
  `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`;
- runner SHA-256:
  `2cd6a5980b5b7e2ce4c8f59b82866f50afd4e7ffadb9ab63dd8e4104bbc9bf0e`;
- exact primary/replica schema-24 SHA-256:
  `c23e1db91a1b601c198e5fe1600480ee2b8f30bb1003e36b244e53588dc9111c`;
  both files are `29,847,552` bytes and byte-identical;
- schema SHA-256:
  `593c6d0736bc9b57c4f5e2c492d1a441f43ab7d4a5206125e3aca1c0ec2d9b11`;
- primary and replica report schema `24`, integrity `ok` and zero FK violations;
- p50 at 1/100/10,000 pages is `15.952/31.930/781.539 ms`; p95 is
  `27.970/46.999/1,028.494 ms`, so 10K remains below the retained Final
  threshold `1,330.972 ms`;
- maximum prepared-statement executions are `8/5/5`; all `69` warmup and
  measurement responses match the independent oracle;
- maximum write executions and per-request `total_changes` deltas are zero;
- measurement DB before/after SHA-256 is exactly
  `1bc060042cab313dcc24fbb87e404238bf2d80897651ebab9a68a518443faae4`
  at `35,127,296` bytes;
- accepted source bytes, SHA and mtime are unchanged.

This is now the current schema/Q10K evidence for migration 024. All earlier
sections remain immutable historical receipts.
