# G8 C80r evidence — schema-24 research-report FTS correction

Status: **PASS**  
Date: 2026-08-14  
Baseline HEAD: `40f5297250cfd214d7fbac2639c013228044e56d`  
Schema: `24` (migration 024 amended before live rollout; migration 025 remains reserved for G9)

## Accepted correction

C80r closes the report-search requirement that was absent from the original C80
packet:

- `research_reports_fts(report_text)` is an external-content FTS5 namespace over
  `research_report_payloads`, keyed by `report_id`;
- exactly two synchronization triggers index insertions and remove the indexed
  row when explicit privacy redaction deletes the payload; report payload update
  remains forbidden and a redacted payload cannot be resurrected;
- ordinary and local Library search use only
  `research_target_heads.current_fresh_report_id`;
- report-to-page mapping is direct for page targets, uses the current Resource
  assignment for Resource targets, and uses persisted run membership for
  selection targets;
- stale, redacted and historical non-head reports never influence search;
- list/count/pagination, public/local bulk IDs and logical-page counts share the
  same parameter-bound predicate.

## Migration and search proof

Focused migration verification proved both fresh schema 24 and `23 -> 24`:

```text
packages/server/test/research-migration.test.ts
2/2 PASS
```

It covers FTS object identity, insert synchronization, blocked payload update,
redaction deletion, blocked resurrection, `integrity_check=ok` and zero foreign
key violations on both migration paths.

The Library integration proves direct page, current Resource assignment,
selection membership, replacement head, stale/redacted/historical exclusion,
page-size-one pagination and bulk-ID/count parity:

```text
research-library-search + content-search + library-bulk-ids
8/8 PASS
local context Library search
1/1 PASS
```

The root-owned combined server run completed with `90` files and `699/699`
tests. An independent read-only auditor reran the focused migration/search files
and server typecheck and reported:

```text
P0/P1/P2/P3:        0/0/0/0
mandatory UNVERIFIED: 0
verdict:             PASS
```

## Strict Q10K-G8 rederivation

Because migration 024 changed, C80r rederived the schema-24 fixture twice and
reran the immutable Q10K-G8 contract. Artifacts are retained at
`.tmp/personal-attention-layer/g8-c80r-strict-20260814T0655/`.

- result SHA-256:
  `2d4c8da41cc46726bc7aad0fa1f8c0adf1abf16f6988c4e9702e84963331c679`;
- official/correctness/performance: `PASS/PASS/PASS`;
- primary/replica SHA-256:
  `2234dd4bcbc91bec1d9b57aff5cc1babd6bb1e5263587a6e0ecf31d71245ded2`,
  exact bytes equal;
- schema SHA-256:
  `0076053033da59de351fe0021523c745f408e68116aa08751771ba58d30ebc14`;
- p95 at 1/100/10,000 pages: `25.226/39.797/863.346 ms`;
- SQL executions `8/5/5`, write executions and `total_changes` delta `0`;
- source unchanged, all independent oracle comparisons pass, integrity `ok`,
  foreign-key violations `0`.

## Full regression and noninterference

```text
corepack pnpm test
shared 58/58; extension 321/321; MCP 108/108; server 699/699; web 477/477
aggregate 1663/1663 PASS

corepack pnpm typecheck
PASS for all five packages

corepack pnpm build
PASS for all five packages

git diff --check
PASS (line-ending notices only)
```

The user runtime was not restarted or migrated: PID `581820` still executes
`packages/server/dist/main.js`, health is `ok`, database is `ok`, schema is `17`,
HEAD remains `40f5297250cfd214d7fbac2639c013228044e56d`, the Git index is empty, and
all three accepted A70 quarantine hashes are unchanged.

**C80r is accepted. C82a is the next ready packet; G8 remains active.**
