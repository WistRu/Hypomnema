# G0 — safety baseline

Verdict: **PASS**  
Accepted at: `2026-08-12T12:23:00+03:00`  
Baseline HEAD: `40f5297250cfd214d7fbac2639c013228044e56d`  
Schema before/after: `17 -> 17`  
Migration head: `017_page_retention.sql`  
Commit: none; changes remain uncommitted by policy.

## Accepted packets

- `C00`: read-only backup/restore fingerprint harness, deterministic S10K fixture, pinned Q0/Q10K contracts, performance runner, fail-closed feature flags.
- `W00`: Library/Graph/canonical-versus-physical regression baseline; no web files changed.
- `A00 -> A01`: MCP/extension protocol and locale baseline plus read-only reconciliation of the pre-existing extension diff; no adapter files changed by these packets.
- Root review: two initial P1 findings were fixed; repeated review concluded `P0=0`, `P1=0`, `P2=0`.

## Resulting implementation paths

- `.gitignore`
- `packages/server/package.json`
- `packages/server/src/attention-feature-flags.ts`
- `packages/server/src/baseline-cli.ts`
- `packages/server/src/baseline-contract.ts`
- `packages/server/src/baseline-database.ts`
- `packages/server/src/baseline-runner.ts`
- `packages/server/test/baseline.test.ts`

No G0 code is wired into normal application behavior. `.tmp/` is ignored so raw DB copies and local logs cannot be added accidentally.

## Immutable contracts and retained artifacts

Command:

```text
corepack pnpm --filter @tabhub/server baseline:g0 -- --samples 20 --work-dir D:\VibeCoding\TabHub\.tmp\personal-attention-layer\g0-baseline-20260812T122100
```

Exit code: `0`.

| Artifact/contract | SHA-256 |
|---|---|
| `Q0-library-v1` | `e62536999fcbd4dd813643da44e9b3832c1d3e327d46945debc8a11a412f6e8a` |
| `Q10K-base-v1` | `e62536999fcbd4dd813643da44e9b3832c1d3e327d46945debc8a11a412f6e8a` |
| `S10K-v1` contract | `d1ffc1866d0ec0ed1047c25c438bb1b2af826e9a2b8743cb31ff4843bd943013` |
| G0 workdir claim | `353e813b03d1e965d72da76d46ee662218a2f42b6950f777678f0b4857860011` |
| `L0-live-scale.sqlite` | `0ea95ce8e10d01d14b50f45a6dd65c9a92c77a78468005b5176b6ec09728e78d` |
| `L0-restored.sqlite` | `0ea95ce8e10d01d14b50f45a6dd65c9a92c77a78468005b5176b6ec09728e78d` |
| `S10K-v1.sqlite` | `d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe` |

Q0 and Q10K are separate frozen arrays even though their v1 contents intentionally match. Their literal hashes and the S10K contract hash are pinned by regression tests. A work directory must be empty and is claimed with an exclusive-create sentinel before any DB artifact is written. An empirical second run against the accepted directory failed before mutation; hashes and timestamps of all existing files remained unchanged.

The consistent SQLite online-backup hash is the snapshot identity. A raw live main-file hash is deliberately not used because WAL activity can make it refer to a different physical state.

## L0 live-scale snapshot and restore proof

- Source: `data/tabhub.sqlite`, opened read-only with `query_only=ON` by the harness.
- Backup/restored size: `4,366,336` bytes each.
- `PRAGMA user_version`: `17` for backup and restored copy.
- `PRAGMA integrity_check`: exactly `ok` for both.
- `PRAGMA foreign_key_check`: `0` violations for both.
- Importance distribution: `{0: 1535}`.
- The complete backup and restored fingerprints are byte-for-byte equal after canonical serialization.

| Table | Rows | SHA-256 row checksum |
|---|---:|---|
| `tabs` | 1535 | `9616f9e4199a3db3625bb5913727b5000a465c1891090bdfebad38c48183b2d9` |
| `contents` | 22 | `9ded2f227a9e5e4f69394064f13d0f0f1114d18b3349d809c56f7ebda589a96e` |
| `contents_fts` | 1535 | `2cc1e45b768a2389984f0890aed31ee658d0bb3d171b398e8a36aed1111f8852` |
| `contents_fts_config` | 1 | `7d6af8d5f3a5b7959afb40dd2dcc5bb8a3f5737f06e0e5f0b9b4fda81e65f1f5` |
| `contents_fts_data` | 41 | `c9401cd7326ac9c0ea0d681086d89ba03a06eeb7ff07f673fb78d6af718374fd` |
| `contents_fts_docsize` | 1535 | `804aaf8274155d5aa3613352dd4b5d196cc5f9faf172b589d7c867901234662d` |
| `contents_fts_idx` | 37 | `f9d836688da92bd3180aa4a9a68ce628631960c135dcf10b456acea8c38ebe19` |
| `tags` | 26 | `4d6179b466dc111abb96f81c20f55bc6b9d01207b896ca8b83ecc1f42f25498b` |
| `tab_tags` | 1622 | `872f8a0ce487599369f34befeb903134ef85735917fb28939dbc603786828adf` |
| `custom_fields` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `embedding_metadata` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `jobs` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `summary_job_attempts` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `tab_instances` | 1027 | `ad77dc219e95290b5d82f2b49515214ef023207a8207c7124192bbd52633329f` |
| `saved_workspaces` | 1 | `052d81c188fcad542a8513efeb46d5b5c85d0ffee8fdf0b2c1e0682c3447720a` |
| `saved_workspace_items` | 1270 | `e6be3bd6a77b92aa2068f9f4e44982e208b63efdab48a1959fa86ff078d4c83f` |
| `knowledge_entities` | 1561 | `e7c4fdd8ee78fdc1840fe4cd3bab4016960941a76abf36822852786cdd6ea0ae` |
| `relations` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `tab_activity_totals` | 196 | `d1e0f64d60e008879227bd1e3c658432e84cb18a9b3fd1259085b44afc91f834` |
| `tab_activity_stream_cursors` | 9 | `b3c485e8e6a4ae83f900d21d7be4236d48ef0678b957f7a4b4934eaa26586ead` |
| `tab_page_activity_totals` | 195 | `a0e5e24213147441d0443dc5ccd5012dd7e28a4e8d7701b9857ca9ccc735b4c6` |
| `page_retention` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `vec_contents` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `vec_contents_chunks` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `vec_contents_info` | 4 | `5771562c241e871084be6c00e48930979999109f7b6caef7611e581bb1413f55` |
| `vec_contents_rowids` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `vec_contents_vector_chunks00` | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

## S10K-v1 deterministic fixture

- Size: `13,721,600` bytes.
- Schema: `17`; integrity: `ok`; FK violations: `0`.
- Importance distribution: exactly `2500` rows at each value `0`, `1`, `2`, `3`.
- Core counts: `tabs=10000`, `contents=5000`, `tags=21` including the guarded default topic, `tab_tags=10000`, `tab_instances=6999`, `tab_activity_totals=6999`, `tab_page_activity_totals=10000`, `knowledge_entities=10021`.
- Core checksums: `tabs=e3042757df4ae4540451a9f20326ddb04707a676eb00992c209a70d6321c2934`, `contents=e0af70e98c58f3a738dcc5f0742929610e0c7423704564d3bfb91d2247d11324`, `tab_tags=9d2a77d95a8b527f3b003c9aaff6595d7c63a2eb6faa8ba1b5dfedea1d0dbee1`, `tab_instances=48f744ff45b4553161ed6dc12f4b4e634f0d3b65cb7318d25d0ec69757e27509`, `tab_activity_totals=6a4dc23b13283e0e820f4547ba919228fe3987c1cf00f88e150113c28c3307a6`, `tab_page_activity_totals=93197a51f0e0531c2937b3172009cf874b7462064e22d5862bdab0bb7debcebf`.
- Two independent generation runs with the same contract produce identical file and row fingerprints; a changed contract seed produces a different tabs checksum.

## Performance baseline and budgets

All timings are in-process HTTP requests after three warmups, `20` recorded samples per query.

| Fixture/query | p50 ms | p95 ms | Accepted later-gate ceiling |
|---|---:|---:|---:|
| L0 default page | 2.936 | 4.322 | 104.322 |
| L0 text search | 14.994 | 18.067 | 118.067 |
| L0 activity sort + open filter | 2.631 | 3.973 | 103.973 |
| L0 tab detail | 0.395 | 0.516 | 100.516 |
| S10K default page | 3.200 | 5.551 | 155.551 |
| S10K text search | 19.805 | 25.867 | 175.867 |
| S10K activity sort + open filter | 22.924 | 33.518 | 183.518 |
| S10K tab detail | 1.060 | 1.447 | 151.447 |

Ceilings use the runbook formulas independently per query:

- L0/Q0: `max(baseline * 1.20, baseline + 100 ms)`.
- S10K/Q10K-base: `max(baseline * 1.25, baseline + 150 ms)`.

The remaining accepted budgets are unchanged: new G3/G6 query p95 at introduction at most `1000 ms`; local mutation p95 at most `500 ms`; live-copy migration at most `60 s`; deterministic priority pass at most `60 s`, batch size at most `100`; AI budget overrun, privacy/safety violations and invalid evidence references exactly `0`; EN/RU parity `100%`; serious/critical automated a11y findings on new surfaces exactly `0`.

## Feature-off and safety proof

Resolved without environment overrides:

```json
{
  "context": false,
  "resources": false,
  "priorityShadow": false,
  "research": false
}
```

Accepted values are only `1`, `true`, `0`, `false` after trim/case-folding. Unknown values throw and therefore fail closed. The parser is intentionally not imported by the application in G0, so normal runtime behavior is unchanged.

No migration ran against the live DB, no live DB row was written by the G0 harness, and raw DB artifacts remain under ignored `.tmp/`. The existing extension continued its ordinary ingest while snapshots were taken; each accepted L0 identity is therefore the atomic online-backup state, not an assumed quiet live-file state.

## Root-wide verification

| Command | Exit | Result |
|---|---:|---|
| `corepack pnpm test` | 0 | server `31/31`, `209/209`; web `50/50`, `246/246`; extension `23/23`, `230/230`; MCP `3/3`, `39/39` |
| `corepack pnpm typecheck` | 0 | shared, server, web, extension, MCP passed |
| `corepack pnpm build` | 0 | server, web, extension, MCP passed; foreground helper rebuilt |
| `git diff --check` | 0 | no whitespace errors |
| `corepack pnpm --filter @tabhub/server exec vitest run test/baseline.test.ts` | 0 | `4/4` focused tests |

The production web build retains its existing chunk-size warning; build exit is `0` and G0 adds no web code.

Fresh post-build runtime proof:

- old process was stopped only after verifying its command line belonged to this workspace;
- new PID: `581820`;
- executable: `C:\Program Files\nodejs\node.exe`;
- command line: `node.exe D:\VibeCoding\TabHub\packages\server\dist\main.js`;
- `GET http://127.0.0.1:7717/api/health`: `{status:"ok", database:"ok", schemaVersion:17}`.

## Web/adapters compatibility proof

- Web focused baseline: `9` files, `62/62` tests; full web suite also passes `246/246`.
- Library remains the default; Graph remains an alternate view; no separate Open Tabs surface was introduced.
- Canonical rows still disclose exact physical copies and exact activation avoids URL-opening duplicates.
- Existing EN/RU catalog parity and keyboard/focus-trap tests pass. No new G0 UI surface exists; the current web package has no axe scanner, so serious/critical automated scanning becomes mandatory when G2 introduces the first new surface.
- MCP protocol remains v4 with `16` tools and `tabhub://tab/{id}`; MCP tests pass `39/39`, typecheck/build pass, built stdio smoke passes.
- Extension EN/RU catalog parity is `50/50`; extension tests pass `230/230`, typecheck/build pass.
- Live browser relay integration is intentionally deferred to packet `A99`; it is not a G0 behavior change.

## Dirty extension quarantine

The following pre-existing user changes were never edited by G0 packets or Root:

| Path | SHA-256 |
|---|---|
| `packages/extension/entrypoints/background.ts` | `977895ccde6939bac2f14dfef005bfab5fda6117292b259a76548a5a5381d364` |
| `packages/extension/lib/tab-command-relay-agent.ts` | `03ddc3b744fa3cd601fb00b9d3e44827fe6a50ad7ae53268a104057602569e58` |
| `packages/extension/lib/tab-command-relay-agent.test.ts` | `4acda614dbb610844cb86eab92b8873930dbb49a605f22d0feccb431743aad72` |

Root materialized the immutable local patch at `.tmp/personal-attention-layer/quarantine-baseline.patch`; SHA-256 is `52ff4898270f40375e103bae6c7ec3d989fdfe9b082552dbb2b1e3bf20348016`. `git apply --check --reverse` exits `0`, proving that the patch describes the current quarantined state.

Reconciled semantics: the background injects fail-closed server reachability into the exact-scope relay; the relay waits for reachability before opening a socket, rechecks connection generation after the async probe, retains retry/backoff, and hands completion to the newest generation. Existing strict scope, idempotent receipt replay, deadlines, serialization and heartbeat tests remain green. The quarantine remains locked until its declared integration packets (`A70`, later `A90`).

## Rollback and residual risk

G0 changed no persisted user data. Code rollback consists of removing the seven new/changed baseline implementation/test entries, removing `baseline:g0` from the server package, and reverting the `.tmp/` ignore entry. The accepted online backup was copied to a disposable restore file, reopened, fully fingerprinted, and proven equal; therefore backup restore is exercised rather than merely documented.

Residual non-blocking facts:

- no new product functionality is enabled yet;
- no new UI exists in G0, so automated axe coverage is deferred to the first UI gate;
- the live DB can continue receiving normal extension ingest, so later migration gates must create a fresh disposable online backup instead of reusing L0 as current state;
- no commit/staging was created.

Required unverified items for G0: `0`. Open P0: `0`. Open P1: `0`.

## Gate conclusion

All C00/W00/A00/A01 handoffs are accepted, full root regression is green, feature-off behavior is proven, backup/restore and performance artifacts are reproducible and retry-safe, and the dirty extension diff remains intact. G0 is accepted as **PASS**. The next ready packet is `C10` under G1 with reserved schema transition `17 -> 18` and migration file `018_logical_pages.sql`.
