# G5 — Shadow PriorityEngine and durable jobs

Verdict: **PASS**  
Baseline HEAD: `40f5297250cfd214d7fbac2639c013228044e56d`  
Current accepted packets: `C50`, `R50`, `C52a`, `C51`, `C52b`, `C53`,
`W50`, `A50`  
Active work: none; G5 closed  
Current schema head: `23`  
Next gate after G5 acceptance: `G6 / C60`

## C50 acceptance

Migration `022_priority_assessments.sql` and the deep PriorityEngine foundation
are accepted after remediation and an independent post-remediation audit:
`PASS / CLEAN`, `P0=0`, `P1=0`, `P2=0`.

- migration SHA-256:
  `e7e0ce815965d68498f9c40fd71de401952629b15024cab61529cef6762bd04c`;
- engine SHA-256:
  `ff13594aef7ed95c55dbbe48bece8affed52339a4a16a1e80cc9f938f0735f67`;
- baseline AST SHA-256:
  `a5bf4436a04dad00bb6ba777169a19096edbdb0cbfb2841e3b338723925df535`;
- focused priority tests: `19 / 19`;
- full server tests: `388 / 388`;
- repository typecheck/build/diff-check: PASS;
- independently observed batch-50 p95: `96.649 ms` (`<= 500 ms`).

The accepted live-derived disposable proof is
`.tmp/personal-attention-layer/g5-c50-proof-20260813T0035/result.json`,
SHA-256
`83934e078bb3f1a75149f5d94a645ef2cb9f7d8bea64d978b44327588a876f60`.
It proves schema `21 -> 22` in `30.49 ms`, all `74` old tables and `15,412`
old rows unchanged, integrity `ok`, zero FK violations, one baseline
ruleset/version/activation and zero outcomes/feedback.

All six original audit findings and the later duplicate-JSON-key finding have
direct regression coverage: timestamp and JSON checks fail closed, SQL/TS AST
parity is strict, stored explanations are revalidated, assessment provenance is
truthful, typed exclusions are method-independent, integer storage is enforced,
and both assessment/feedback writers use `BEGIN IMMEDIATE` semantics.

## R50 pre-assessment receipt

`R50` is complete. Before any real collection assessment, Root fixed the UAT
sample seed, its derivation and the exact stratum/ranking/fallback algorithm in
[`R50-seed-receipt.json`](R50-seed-receipt.json). The receipt is anchored to
the accepted migration/AST hashes and to the C50 proof whose six outcome and
feedback tables are all empty. Its SHA-256 is
`a7408381fac94fe140df1b4a5cc1ac3222cabfc35561b24b6bceb7bb4b9addf0`.
This prevents selecting the UAT sample after seeing assessment results.

## C52a interface decision

Before implementation, Root accepted runbook decision #4 and the matching ADR
dated `2026-08-13`. It fixes the additive namespaced job identity, keeps the
numeric legacy summary contract and migration-004 tables unchanged, defines the
schema-23 new-ledger state machine, idempotency, cooperative cancellation,
lease fencing/recovery, typed errors, feature authorization and pure `4:2:1`
fair scheduling. Exact priority/research runtime limits remain injected by
their owning consumer packets rather than being invented by the facade.

## C52a acceptance

`C52a` is accepted on the exact frozen v3 packet. It adds schema 23, the
`DurableAiJobs` facade/ledger, immutable attempts/events/idempotency receipts,
typed priority/research/summary envelopes, bounded registered completion guards,
atomic lease/checkpoint/terminal transitions and pure `4:2:1` scheduling. The
legacy numeric summary contract remains available through its typed adapter.

Frozen implementation SHA-256 values:

- `023_ai_jobs.sql`: `0badc9f4738f47c2205ef0e7f994409d98f5e22e4dff6ad5fbee6c02a3d800a2`;
- `ai-job-ledger.ts`: `6068c78c0585c838b1c09c1f7e551fd20266512e746910642d22482aab5337f7`;
- `durable-ai-jobs.ts`: `223c645891220ea6745f3c7cc12868ae09b2d0738925d9250d6feb22208720b1`;
- `ai-job-scheduling.ts`: `0c052fdb224660feb9d1af1857bf51f2340b076bb46e0a7b12e4b43730d3b545`;
- `summary-job-adapter.ts`: `d5b24dbf1186ad460e87483cff63854043abfe5d0beb177afa35247506608c5a`;
- minimally extended `summary-catalog.ts`:
  `50018fed4c22344dec6ecc62657f80561fc98beacf3bfc623995bde93551bec3`.

The live-derived disposable proof is
`.tmp/personal-attention-layer/g5-c52a-proof-20260813T0335/result.json`,
SHA-256
`fbeb98abf8bc4728300c81c99661f329ee14b83752709d790ff0636e8e1594cd`.
Its verifier SHA-256 is
`33f67d8645d4c7ba9465beb81aa6d42a816950dbdbe960aee2285e6b732397fb`.
It proves schema `22 -> 23` in `57.75 ms`, all `83` old tables and `15,415`
old rows byte-canonically unchanged, four new job tables empty, integrity `ok`
and zero FK violations. The source database hash is unchanged before/after the
copy proof.

Acceptance verification:

- full server suite: `55` files, `427 / 427` tests;
- shared suite: `4` files, `18 / 18` tests;
- focused SQL invariant proof: `3` files, `39 / 39` tests;
- repository typecheck/build/diff-check: PASS;
- facade batch-50 p95: `0.330 ms` in independent acceptance (`<= 500 ms`);
- independent implementation/API audit: `PASS / CLEAN`, `P0=P1=P2=0`;
- independent exact-hash SQL audit: `CLEAN`, `P0=P1=P2=0`.

Direct regressions prove fail-closed model provenance, exact five-field usage
deltas, immediate final-progress adjacency, UTC-day cost accounting, immutable
global receipts, cancellation/bind fences, interrupted recovery attempts,
queued/running supersession separation, ledger-owned partial fingerprints and
bounded registered guard accounting.

The extension quarantine hash remains
`cb1a7f60010ec8fd5cbcde5d7ae554adf99e4cf1c9de2e152be57c65b583c60a`.
The live user runtime was not migrated or restarted: PID `581820` remained
healthy at schema `17`. No staging, commit or push occurred.

## C51 and amended C52a joint acceptance

`C51` is accepted together with the narrow C52a liveness extension recorded in
runbook decisions #4/#5 and the `2026-08-13` ADR clarification. The accepted
implementation provides one canonical page/Resource feature projection,
derived dirtiness, fixed registered guarded completion, bounded single and
collection recompute, local/agent provenance-safe routes, batch reads and a
stable keyset review queue. It does not write user importance, Resource user
evaluation or retention state.

The C52a extension keeps cumulative execution state ledger-owned through the
read-only validated `refresh(claim)` envelope. Reserved ledger guard cycles,
row/byte overflow proof and partial fingerprints cannot be forged by the C51
consumer. Scan, denominator enumeration, preflight and atomic guard wall/step
usage are all bounded and durably accounted. Exact denominator growth
`I=3000 -> N=14000` terminates as non-coverage `partial` at `34002/36000`
steps instead of throwing or leaving a running job. Empty collections succeed
with zero invented work.

Frozen implementation SHA-256 values:

- `ai-job-ledger.ts`: `e2bbc73983308ea2867bf6f302c60d1b8b093d6c02eb7536c9381f04f6e6be1b`;
- `priority-engine.ts`: `2977fbadb023884d79ddaa2a126b2b5dda0034ec8914ef949d707a0f0d6acab7`;
- `priority-feature-catalog.ts`: `15a7bf322db51edfdf89a6698547011456f3928a723951b5edfc1d1b37710f1d`;
- `priority-assessment-coordinator.ts`: `4e84878630d55981de28f7a32e1b41e54d186e5c2d31fc3a3f218edd1edefbd6`;
- `priority-routes.ts`: `f94727277a9c9dabae7a377c6cc09599c3305cd9bcbf9be53c2303b22ecad541`;
- `app.ts`: `40a2967a5963d12f1b27fc2d94d8bba4920d892b7b875365ac3b7e17c317ce85`;
- shared contracts: `499dde277a57ad377065dd405208f355f59af622ee53f6edd6b05ac1599f9949`.

Acceptance verification on the exact frozen tree:

- full server suite: `58` files, `493 / 493` tests;
- shared suite: `5` files, `22 / 22` tests;
- independent core acceptance: `105 / 105` tests;
- independent migration/scheduling/routes/flags acceptance: `19 / 19` tests;
- coordinator acceptance: `38 / 38` tests;
- repository typecheck, build and diff-check: PASS;
- independent final audit: PASS, `P0=0`, `P1=0`, `P2=0`.

Direct race tests cover every fixed guard source family, local-only
non-invalidation, cancellation, restart/fixpoint cycles, denominator
growth/shrink, intermediate guard overflow, inactive Resource identity and
full user-field non-interference. The extension quarantine hash remains
`cb1a7f60010ec8fd5cbcde5d7ae554adf99e4cf1c9de2e152be57c65b583c60a`.
The live runtime stayed untouched and healthy at schema `17`; no stage, commit
or push occurred.

## C52b integrated runtime acceptance

`C52b` is accepted after an independent frozen-tree audit with
`P0=0`, `P1=0`, `P2=0`. One common runtime now owns both available physical
adapters. A successful physical claim advances the persisted seven-slot
`4:2:1` cursor in the same SQLite transaction; failed, deferred, unavailable,
invalid-boundary and rolled-back claims do not advance it. The application no
longer starts a competing legacy summary drain: summary routes wake the common
runtime, while provider-off and priority-writer-off configurations remain
write-free.

The amended migration 023 and current composite implementation hashes are:

- `023_ai_jobs.sql`: `f74cfb8556aed4df9c67b7e19026664661fb4df3a366d33a86a7aa34c841a497`;
- `ai-job-runtime-state.ts`: `48e4fc70e0f4ac9a8502751a73424e86e57697580e9ad9bdb35404bf3583ed05`;
- `ai-job-runtime.ts`: `b07922897c72ccfc0faab82931223e1d6737b2932dcf16e119b9cb0a79d5c300`;
- `ai-job-ledger.ts`: `263ae7295ad04961c05820aecd6426774f12668f81181c6753bb05975ba1805a`;
- `summary-catalog.ts`: `3d29b8f90630b7870f8cfb54a07c89bd2619a7e20265773cad7e9dd8bf40a5f0`;
- `summary-worker.ts`: `76f0c4dbc9ade30cb8e737e7e922889698384adecc9c23c0ce9806a1c61422c0`;
- `summary-routes.ts`: `3c7fe16e40b519de97209e424143d2e2c988a58a33f054dd799a6a32516d9519`;
- `priority-feature-catalog.ts`: `2e9542dc286369298e119be4425d66d16a5bc3eb1686ad209a3ff683f402a650`;
- `priority-assessment-coordinator.ts`: `176db75ee554e691a8091cf47811234fa09e23000507d69e268faaf87fa8a1e0`;
- `priority-routes.ts`: `146d405536ef2854e0c4bd0894acadadf38f74a1fc4ad5031b0f50405e7fdc29`;
- `app.ts`: `8b736153503b466380c6f6a87271d91acdc5d566b9eb2269b88f61a7da1d3637`.

The refreshed live-derived disposable proof is
`.tmp/personal-attention-layer/g5-c52b-proof-20260813T0645/result.json`,
SHA-256
`f2bfd3b4c74c711afbcc4f1bd9cb50bcc6f00eca5a1016f724460f38a380542e`.
Its verifier SHA-256 remains
`33f67d8645d4c7ba9465beb81aa6d42a816950dbdbe960aee2285e6b732397fb`.
It proves schema `22 -> 23` in `33.794 ms`, all `83` old tables and
`15,415` old rows byte-canonically unchanged, and five expected new tables:
the four empty new-ledger tables plus the seeded singleton
`ai_job_runtime_state`. Integrity is `ok`, foreign-key violations are `0`, and
the source database hash is unchanged before and after the copy proof.

Direct integration evidence covers two real saturated cycles, borrowing while
research is unavailable, both physical-ledger reopen/recovery paths, a priority
restart from a durable collection checkpoint, summary provider abort on stop,
queued/running cancellation, cooperative lease renewal at the exact
`19/20/59-second` boundaries, strict `60-second` lease-loss recovery and stale
token fencing. Collection enumeration is keyset-paged at most 100 subjects;
feature reads are fenced before semantic assessment; known cancellation usage
is checkpointed exactly. Reader/shadow-only startup leaves the runtime cursor
and job tables unchanged, while writer-only app wiring drains a real priority
job through the common runtime.

Acceptance verification on the frozen tree:

- independent focused matrix: `10` files, `135 / 135` tests;
- full server suite: `61` files, `514 / 514` tests;
- server typecheck, build and repository diff-check: PASS;
- independent implementation audit: PASS, `P0=0`, `P1=0`, `P2=0`.

The historical C52a and C51 hashes above identify their earlier accepted
snapshots; the C52b composite hashes in this section are authoritative for the
current G5 core tree. The extension quarantine hash remains
`cb1a7f60010ec8fd5cbcde5d7ae554adf99e4cf1c9de2e152be57c65b583c60a`.
The user runtime was not migrated or restarted: PID `581820` remained healthy
at schema `17`. No stage, commit or push occurred.

## C53 client-contract bridge acceptance

`C53` and the `C53-R2` receipt hardening are independently accepted with
`P0=0`, `P1=0`, `P2=0`, `P3=0`.

- Every Library `TabListItem` has a required positive `logicalPageId` from the
  canonical row mapper. Physical/browser `id` remains the physical-tab
  identity, and duplicate browser copies expose the same logical identity.
- `PUT /api/agent/user-importance` is the single strict page/Resource agent
  write seam. It requires typed subject, `authorizationRef` and
  `idempotencyKey`, records `agent/on_behalf_of_user`, projects page changes to
  every physical copy, and keeps Resource evaluation canonical.
- The write and append-only receipt are one transaction. Namespaced receipt
  identity, semantic request fingerprinting, result timestamps, subject FKs,
  immutability triggers and `created_at >= result_updated_at` are enforced by
  migration 023.
- The optional capability `agentUserImportance` fails closed when omitted and
  is advertised only at schema 23 when logical importance or Resources are
  available. It is independent of the three priority rollout flags.

Authoritative composite hashes:

- `023_ai_jobs.sql`:
  `658fa338613e9d9cfcd90ae1647ebe42cbb5c4a1ef9a144b604b027ca9ac88c3`;
- `agent-user-importance-catalog.ts`:
  `e144455a4b71f99e9d6774bbce0b265e3f16b547adc399f4ec01cc5d52e1ccdb`;
- `agent-user-importance-routes.ts`:
  `c736acfcfd393fe6ec2665c2da3f38541cc771a6bd7966afe8ceeef4e81667ec`;
- `attention-feature-flags.ts`:
  `bc86650341bb7ce28d568dbc2139d858203afd61229a00caa72d00da70baaba5`;
- shared contracts:
  `fc9cc2253dc41b37e83ecb7197b4232506c920d05743910cc1650c5186ebb943`;
- server `app.ts`:
  `41e65192e66273d5d020bd5a76340d804bdeff4980f2d71224d642b433c31332`.

The authoritative migration proof is
`.tmp/personal-attention-layer/g5-c53-r2-proof-20260813T0752/result.json`,
SHA-256
`fe0526c281e6b141e5884f9a0d57ad15579b0bff3ed6b49f03ee9d40a89c11a8`.
Its verifier SHA-256 is
`33f67d8645d4c7ba9465beb81aa6d42a816950dbdbe960aee2285e6b732397fb`.
It proves schema `22 -> 23` in `26.913 ms`, all `83` old tables and `15,415`
old rows unchanged, exactly six new tables, integrity `ok`, FK violations `0`,
and an unchanged source hash. Source and target DB hashes are respectively
`af4a9ba7509ad03c42010bc0cc9dca6ff0eacd7742430fc593cc5137ed94859e`
and `ab1c05f1307c342dc74343d596303f31258435bceaf937fc90df3c3c88d1a6e4`.

Focused C53 verification passed `6` files / `67` tests; the then-current full
server suite passed `62` files / `522` tests. Server/shared typecheck, build and
diff-check passed.

## W50 shadow UI acceptance

`W50`, its stale-cache remediation and real-REST `W50-REST2` proof are
independently accepted with `P0=0`, `P1=0`, `P2=0`, `P3=0`.

- Shadow badges and explanations distinguish `You`, `AI`, missing signals and
  typed exclusions without enabling priority sorting or personal rules early.
- Feedback and a mixed page/Resource review queue are paginated and bounded to
  batches of at most `100`. Retry paths preserve exact subject and provenance.
- Feature/query errors clear cached success data and reset the queue rather
  than presenting stale shadow state.
- EN/RU, keyboard and automated accessibility coverage pass; there are no
  retention or destructive controls in the shadow surface.
- The REST integration uses the real schema-23 `createApp` server. It captures
  default Library order before manual importance/recompute, proves post-run and
  rendered App order exactly equal that initial order, opens the real Drawer,
  and validates the cumulative transcript against a strict safe allowlist. The
  only permitted POST is `/api/personalization/priority/read-batch`.
- A same-DB flags-off restart keeps manual importance but exposes no shadow
  surface and issues zero priority POST requests.

Authoritative hashes include:

- `PriorityShadow.tsx`:
  `a6cd7ada3b3415786cacd1ab58beca70dc1067dbc6db216fa69019e6888482d6`;
- `PriorityShadow.css`:
  `7e86446e0a2f2d94f4bd7ba48e8afae42e03673ae2ec445c3249c19e4369a017`;
- `priority-shadow-rest-integration.test.tsx`:
  `74effddb14462db66a51c6eb541d3c552069d96a8464d22876ab2bcac8f1ac20`;
- `priority-rest-server-worker.ts`:
  `c1df9f3afd96a0622b9573799129e8ce7d8a0f84f68a93e816ce234892ec77ae`.

The focused actual-REST proof passed `1 / 1`; the full web suite passed `64`
files / `359` tests. Web typecheck, build and diff-check passed. Independent
inspection confirmed that the server-backed test worker is absent from the
production bundle and leaves no temporary process or database behind.

## A50 MCP acceptance

`A50` and `A50-R2` are independently accepted with
`P0=0`, `P1=0`, `P2=0`, `P3=0`.

- `explain_priority`, `record_priority_feedback` and `set_user_importance`
  expose typed page/Resource behavior with safe provenance and strict schema.
- User-importance mutation requires the explicit server capability
  `agentUserImportance=true`; old servers and omitted capabilities fail closed.
- Feedback correlation and nested assessment/exclusion subject identity must
  exactly match the top-level subject.
- Error text rejects C0, DEL, C1, U+2028 and U+2029 controls and does not expose
  raw server bodies or secrets.
- Direct API, SDK and freshly built stdio protocol negatives pass.

Authoritative implementation hashes:

- `main.ts`:
  `424fdb9655e22725c736f97756f58e315112e81ed341dd86ab72c60151ce1aad`;
- `server.ts`:
  `106219c0867735084cae1c630d5ec8d9e89a90f90042f966edd9c1508c44b455`;
- `tabhub-api.ts`:
  `a709f37bfe9b3153951f2c929fb064aa93e8e76872d0778c6bc1f6e5704eb510`;
- `priority-projection.ts`:
  `041f3aebfa6aaef3cee91e3eb748e07fcd908ea4034b63ad8bed91fd31f68258`.

The full MCP suite passed `8` files / `100` tests. A fresh post-build stdio run
of `test/priority-stdio.test.ts` passed `1` file / `3` tests.

## Full-corpus priority proof

The disposable full-corpus proof is independently accepted with
`P0=0`, `P1=0`, `P2=0`. Its only P3 recommendation is to include several
additional non-priority table hashes directly in a future receipt; independent
source-to-final comparison already proved those tables unchanged.

Artifacts:

- result:
  `.tmp/personal-attention-layer/g5-final-20260813T0800/result.json`, SHA-256
  `749783ecadf5812c69168ef3c6baabe0547a2f2b5515771b2efb29182aa29464`;
- verifier SHA-256:
  `a83eabef63e89a93d31a595d557fbd49119f6a2f546f793b5ffe29932d2f298e`;
- assessed DB SHA-256:
  `023eccca8f72c2caa89c90e524ebe8d7a5d8f5573855066751afdd13645fabdf`;
- immutable source C53-R2 DB SHA-256:
  `ab1c05f1307c342dc74343d596303f31258435bceaf937fc90df3c3c88d1a6e4`.

Preparation resolved `1,432` of `1,535` logical pages, persisted `57` unmatched
and `46` excluded reasons, and produced `274` active Resources. The exact
denominator is `1,535 + 274 = 1,809`.

Both deterministic runs succeeded in `3,326.186 ms` and `2,729.905 ms`, with
real batches capped at `100`, `5,427` accounted steps each, zero tokens/cost and
the same result fingerprint
`398ebdd209581624a63ca02cf2fc373ac8aaaf621aba8622fa4314af7f92a5dd`.
The current outcomes are exactly `1,479` page assessments XOR `56` page
exclusions plus `274` Resource assessments; duplicates, overlap, invalid kinds
and outcomes outside the denominator are all `0`. All revisions remain `1` on
the repeat run.

The independent auditor reran the 15-column guard SQL for both jobs and found
zero source/outcome, ruleset, identity, XOR or semantic mismatches. Page and
Resource manual values, all `1,558` legacy tab importance values, retention,
physical-tab state and importance receipts were unchanged. Integrity is `ok`;
FK violations are `0`; the sanitised receipt contains no raw URL, title,
content, context, credential or idempotency value.

## Rollback and feature-off proof

The disposable ON/OFF rollback drill is
`.tmp/personal-attention-layer/g5-final-20260813T0800/rollback/rollback-proof.json`,
SHA-256
`a7277fc39798e90da4c85084f142151863633511bab9ab78c18ea3a319e0179c`.
The proof script SHA-256 is
`e30404ead00c768758842dac4d97265888ae8131198be9cd3d29452d1a3bcc24`.
Source hash before/after is
`023eccca8f72c2caa89c90e524ebe8d7a5d8f5573855066751afdd13645fabdf`;
the final ON/OFF DBs are byte-equal at
`8f94ffd5e0217e82231a8c4dc76da518298d520584a3b4b931b8709bba570e98`.

- All `4,197` non-priority HTTP responses are exactly equal in ON/OFF modes.
- Default and importance ASC/DESC Library pagination exactly match independent
  SQL oracles over all `1,558` tabs.
- All `1,809` persisted outcomes remain present with flags off but every
  priority reader/status/recompute route fails closed with `404`; five selected
  ON read/status routes return `200`.
- `/api/features` differs only in the priority writer/reader/shadow triplet;
  `agentUserImportance=true` remains available in both profiles.
- No queued/running job existed before the read drill. Reads and close/checkpoint
  produce zero delta across all `90` tables. Separate protected hashes cover
  jobs, outcomes, feedback, runtime cursor, manual preferences, retention,
  resource commands and importance receipts.
- The only identical startup delta is the expected G4 daily-writer lifecycle
  transition. Every 30d page/Resource response honestly reports partial rather
  than full coverage.
- The source's actual manual page and Resource values are both `2`; both are
  visible and unchanged in ON/OFF modes. A prior test prompt's literal `3` was
  not a runbook requirement.
- Integrity is `ok` and FK violations are `0` on both copies.

Independent rollback acceptance is PASS with
`P0=0`, `P1=0`, `P2=0`, `P3=0`. The auditor recomputed every artifact hash,
all 90-table and 17 protected-table fingerprints, the three SQL order oracles,
XOR coverage, route gating, sanitization, integrity and FK results.

## Performance evidence

Root reran all immutable query families with three warmups and `20` recorded
samples per query. Independent performance acceptance is PASS with
`P0=0`, `P1=0`, `P2=0`, `P3=0`.

The authoritative G0 receipt is
`.tmp/personal-attention-layer/g5-final-20260813T0800/performance-g0-receipt-20260813T0818/receipt.json`,
SHA-256
`0732cb6114c971c0e4f4fce823f6cffe3283c0acb1327a01d5cc0d6ae92ff237`.
It supersedes the earlier receipt-less G0 directory and distinguishes the exact
schema-17 backup/restore boundary hash
`3128ff63178e721baa9093b735dbabefe53652db300b26c38b61528f528ea8bd`
from the disposable retained restore after current-code measurement migration
(`ab47714366b8cddd4659cf69a4645d03adbcd32f0bce94185de51226ef112454`,
schema 23). The live source was not migrated or restarted. The generated
schema-23 S10K hash is
`b71b0c4c88ff43e689f3e940bf36fb6680e8af8368f29c73357244556cf678b3`;
integrity is `ok` and FK violations are `0`.

| Q0 live query | G5 p95 ms | accepted ceiling ms | Result |
|---|---:|---:|---|
| default page | 7.772 | 104.322 | PASS |
| text search | 34.702 | 118.067 | PASS |
| activity sort + open filter | 4.908 | 103.973 | PASS |
| tab detail | 1.072 | 100.516 | PASS |

| Q10K-base query | G5 p95 ms | accepted ceiling ms | Result |
|---|---:|---:|---|
| default page | 4.063 | 155.551 | PASS |
| text search | 24.124 | 175.867 | PASS |
| activity sort + open filter | 36.103 | 183.518 | PASS |
| tab detail | 1.992 | 151.447 | PASS |

The official G3 artifact is
`.tmp/personal-attention-layer/g5-final-20260813T0800/performance-g3/Q10K-G3-v1.json`,
SHA-256
`1123bf594a4f7119fd870383af234146d4ac5f160579d68bb5d8d79c265911d3`.
It is `official=true`, `passed=true`, `performancePassed=true`; immutable source
hash
`d5cb2f53d3ba7bca7cf764b17ad7893536df5ffe29e131a65765848a5d71eefe`
and mtime `1786526476493.541` are unchanged. Primary, replica and measurement
derivations are byte-equal at
`f3769a659bdf89232575a46309d4582eb483be0642a92b404cca5d9ee74367bf`,
Topics are preserved, integrity is `ok` and FK violations are `0`.

| Q10K-G3 query | G5 p95 ms | later-gate ceiling ms | Result |
|---|---:|---:|---|
| resource list intersection | 83.935 | 182.933 | PASS |
| resource detail | 120.859 | 205.535 | PASS |
| resource pages intersection | 184.178 | 246.335 | PASS |
| resource all-time activity | 72.702 | 247.790 | PASS |
| Library Resource + Topic | 9.684 | 110.936 | PASS |

The focused baseline verifier also passed `2` files / `7` tests.

## Resulting-tree regression evidence

The following commands ran on the resulting current G5 tree with exit code `0`:

- `corepack pnpm test`: shared `5` files / `24` tests, extension `28 / 283`,
  MCP `8 / 100`, server `62 / 522`, web `64 / 359`; total `167` files and
  `1,288` tests;
- `corepack pnpm typecheck`: all five packages PASS;
- `corepack pnpm build`: server, web, MCP and extension production artifacts
  PASS;
- `corepack pnpm --filter @tabhub/mcp exec vitest run
  test/priority-stdio.test.ts`: `1` file / `3` tests PASS against the freshly
  built stdio artifact;
- `git diff --check`: PASS.

The full-suite performance probes were also within budget:
`C50_BATCH50_P95_MS=140.193` and `C52A_FACADE50_P95_MS=0.860`.
The extension quarantine hash is still
`cb1a7f60010ec8fd5cbcde5d7ae554adf99e4cf1c9de2e152be57c65b583c60a`.
The live user runtime remains untouched at PID `581820`, health/database `ok`,
schema `17`. No stage, commit or push occurred.

## Independent G5 gate audit

Final independent verdict: **PASS**. Root may advance to G6.

- Severity: `P0=0`, `P1=0`, `P2=0`, `P3=1`.
- Required `UNVERIFIED` items: `0`.
- The sole P3 is the already disclosed receipt-hardening recommendation for
  future full-corpus runs; independent source/final comparison proves the
  non-priority tables unchanged in this gate.
- Fresh focused audit runs passed core `10` files / `143` tests, W50 `2` files /
  `12` tests and A50 `3` files / `30` tests.
- The auditor independently rechecked current/artifact/quarantine hashes,
  live PID `581820` health at schema `17`, and `git diff --check`.

All eleven gate-exit conditions are satisfied: every packet and touched
interface is accepted; scoped and full checks pass; the cross-surface REST/MCP
invariants pass; feature-off behavior preserves G4; migration passes on fresh
schema-22 and live-derived copies; EN/RU/a11y and privacy negatives pass;
rollback was executed on disposable copies; open P0/P1 and required unverified
items are zero; this manifest is PASS.
