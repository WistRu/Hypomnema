{
  "name": "@tabhub/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./limits": "./src/limits.ts"
  },
  "scripts": {
    "test": "node -e \"require('fs').rmSync('.vitest-report.json',{force:true})\" && vitest run --reporter=default --reporter=json --outputFile.json=.vitest-report.json --maxWorkers=4 && node ../../scripts/check-test-completeness.mjs . src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
